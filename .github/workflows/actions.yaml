name: Running commands on the Runner
on:
 push:
   branches:
     - main
     
jobs:
  basic:
   runs-on: ubuntu-latest
   steps:
    - name: Greeting GitHub Actions
      run: echo "Hello Github Actions!!"
    - name: printing Hostname
      run: echo $(hostname)
    - name: printing id
      run: echo $(id)
      
  
